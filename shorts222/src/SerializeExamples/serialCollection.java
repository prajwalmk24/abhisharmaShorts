package SerializeExamples;

import java.io.*;
import java.util.Arrays;
import java.util.List;

public class serialCollection {

    public static void main(String[] args) throws IOException {
/*

        List<Student> students = Arrays.asList(new Student(1,"www"), (new Student(2,"ww2w")));

        // ✅ Serialize the list
        try (ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("stu.ser"))) {
            oos.writeObject(students);
            System.out.println("List serialized."+students);
        } catch (Exception e) {
            e.printStackTrace();
        }

        // ✅ Deserialize the list
        try (ObjectInputStream ois = new ObjectInputStream(new FileInputStream("stu.ser"))) {
            List<Student> deserializedStudents = (List<Student>) ois.readObject();
            System.out.println("List deserialized:"+deserializedStudents);
        } catch (Exception e) {
            e.printStackTrace();
        }


 */
    }

}
